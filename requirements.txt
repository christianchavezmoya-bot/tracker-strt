# HOLO-RTLS — Python Dependencies
# Python 3.10+

# Web framework
Flask==3.0.3
Flask-CORS==4.0.1
Jinja2==3.1.4

# Database
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
SQLAlchemy==2.0.35
# SQLite is built-in; production Postgres:
psycopg2-binary==2.9.9

# Authentication & Security
Flask-JWT-Extended==4.6.0
PyJWT==2.9.0
argon2-cffi==23.1.0
pyotp==2.9.0
qrcode==7.4.2
Werkzeug==3.0.4
cryptography==43.0.3   # Optional Fernet backup encryption (BACKUP_ENCRYPT_KEY)

# Networking
paho-mqtt==1.6.1
requests==2.32.3

# Serial / Hardware
pyserial==3.5
bleak==0.22.2        # Cross-platform BLE (Linux/macOS/Windows)
pybluez==0.23        # BLE on Linux (apt install libbluetooth-dev first)
bluepy==1.3.0        # Alternative BLE scanner for Raspberry Pi

# Email
Flask-Mail==0.10.0

# Background tasks
APScheduler==3.10.4

# API docs
flasgger==0.9.7.1

# Security & Performance
Flask-Limiter==3.5.0
Flask-Talisman==1.1.0
Flask-Compress==1.15

# Utilities
python-dotenv==1.0.1
marshmallow==3.21.3
Pillow==10.4.0          # For logo image processing

# Testing
pytest==8.3.3
pytest-flask==1.3.0
pytest-cov==5.0.0

# Production server
gunicorn==22.0.0

# ─── Reference / Hardware modules (from tracker-strt reference/) ─────────────
# These are imported directly from the reference/ folder, not pip-installed.
# They are: uwb_positioning.py, uwb_serial_reader.py, floor_plan_mapper.py
numpy==1.26.4   # Required by reference/uwb_positioning.py
